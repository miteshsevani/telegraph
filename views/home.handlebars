<div class="content-wrapper">
  <header>
    <h1 class="headline">{{{ article.post.title }}}</h1>
    <div class="stand-first">{{{ article.post.standfirst }}}</div>
    <div class="publish-date">{{{ postDate }}}</div>
  </header>
  <div class="article-wrapper">
    <img class="hero" src='{{{ article.post.image }}}'>
    <article>
      {{{ article.post.body }}}
    </article>
    <div class="comments-wrapper">
      <div class="comments-header">
        <div class="title"><span class="comments-length"></span> Comments</div>
        <div class="sort">
          Sort:
          <span class="like">Likes</span>
          <span> | </span>
          <span class="like">Newset</span>
        </div>
      </div>
      <div class="comments"></div>
    </div>
  </div>
  <aside class="side-rail">
    <h3>More Stories</h3>
    <div class="associated-articles">
      {{#each posts}}
      <div class="story">
          <img src="{{ thumbnail }}">
          <div class="content">
            <h4 class="title">{{ title }}</h4>
            <a href="#" class="title">Read more</a>
          </div>
      </div>
      {{/each}}
    </div>
  </aside>
</div>